# The committed panel bundle is generated by Vite from frontend/src and
# inlines Lit itself; CodeQL should analyse the TypeScript sources, not the
# minified vendor output (it flags Lit's internal HTML-comment regex there).
paths-ignore:
  - custom_components/kubernetes/frontend/**
